import 'package:flutter/material.dart';

class Download extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Download", style: TextStyle(fontSize: 30.0),),
    );
  }
}
