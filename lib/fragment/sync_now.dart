import 'package:flutter/material.dart';

class SyncNow extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Sync Now", style: TextStyle(fontSize: 30.0),),
    );
  }
}
